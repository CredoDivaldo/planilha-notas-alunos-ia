# Story 8.5 — Remove Legacy Express Tree

**Epic:** 8 — Production Cutover & Hardening
**Status:** Done
**Priority:** P0
**Points:** 2
**Executor:** @dev
**Quality Gate:** @qa (auto-PASS, see AC verification)
**Sprint:** Cutover
**Depends On:** 8.1, 8.2, 8.3 (FastAPI + Vite/React cutover complete)
**Merged:** PR #5 — squash-commit `079a041` on `master`

---

## Story Statement

**As a** platform operator,
**I want** the entire legacy Express tree (`src/`) and old MVP single-page UI (`public/{index.html,app.js,styles.css}`) removed, the root `package.json` updated to no longer reference Express, and the root `README.md` rewritten to reflect the FastAPI + React/Vite stack,
**so that** the production code base is unambiguous about its architecture (FastAPI backend + Vite/React frontend), there is no dead `src/server.js` start path, and `npm install` no longer pulls in Express dependencies.

---

## Acceptance Criteria

- [x] **AC1:** `git grep "require(\"express\")|src/server.js|src/app.js"` returns 0 matches in production code.
- [x] **AC2:** Root `package.json` no longer lists `cors`, `csv-parse`, `dotenv`, `express`, `express-rate-limit`, or `multer` in `dependencies` (or any deps/devDeps).
- [x] **AC3:** `pytest backend/tests/` is green (167 passed).
- [x] **AC4:** `npm run dev` is the only start path; no `npm start`, no Express route, no Express entry in `package.json`.
- [x] **AC5:** Root `README.md` exists and reflects FastAPI + React/Vite stack (no Express, no MVP single-page UI).
- [x] **AC6:** `public/app/` (Vite build output) is preserved — not deleted.

---

## Changes

### Removed (32 files, ~3,350 deletions)

- `src/` (entire Express tree):
  - `src/app.js`
  - `src/server.js`
  - `src/config/validate-env.js`
  - `src/middleware/{auth,request-id}.js`
  - `src/routes/{evolution,grades,match,send,students}.js`
  - `src/services/{csv-parser,csv-validator,evolution-client,matcher,storage}.js`
  - `src/utils/async-handler.js`
- `public/{index.html,app.js,styles.css}` (old MVP single-page UI)
- `tests/{routes,services,middleware}/` (Express-targeted test dirs)
- `tests/{critical-flow,accessibility,form-validation}.test.js` (Express-targeted tests)

### Modified

- `package.json`:
  - `description` → "Plataforma Académica — FastAPI + React"
  - Removed `dependencies`: `cors`, `csv-parse`, `dotenv`, `express`, `express-rate-limit`, `multer`
  - Removed `main: src/server.js`
  - Removed `start: node src/server.js` (no Express entry)
  - Removed `devDependencies` no longer needed at root: `axe-core`, `jest`, `jest-environment-jsdom`, `supertest`
  - Lint scope narrowed from `src/**/*.js` to `tests/**/*.js` (no `src/` anymore)
  - Kept `prepare: husky` (`.husky/pre-commit` still present)

### Created

- `README.md` (root): documents the FastAPI + React/Vite stack, quick start, structure, and command reference.

### Stays (preserved)

- `backend/`, `client/`, `outputs/`, `docs/`, `legacy/`
- `tests/frontend/`, `tests/integration/`, `tests/public/`, `tests/config/`
- `public/app/` (Vite build output, regenerated by `npm run client:build`)
- `docker-compose.evolution.yml`, `alembic.ini`, `pyproject.toml`

---

## AC Verification (run on master @ `079a041`)

```
pytest backend/tests/  →  167 passed
git grep -l 'require("express")|src/server.js|src/app.js' \
  -- ':!node_modules' ':!.claude/worktrees' ':!legacy' ':!*.md' ':!tests/' \
  → (no output) — OK, no Express refs in production code
ls src/                →  only .gitignored build artifacts (egg-info), no tracked files
ls public/app/         →  Vite build intact (assets, favicon.svg, icons.svg, index.html)
```

`tests/integration/rate-limit.test.js` (a self-contained third-party library behavior test that boots a tiny express app to validate `express-rate-limit`) is preserved by explicit scope and currently fails to import with `express` removed. Flagged for follow-up story.

---

## Commits

1. `3114dfa` — `chore: remove legacy express tree and tests [Story 8.5]` (32 files, 3,350 deletions)
2. `1d211e1` — `chore: update root package.json + README for fastapi+react stack [Story 8.5]` (package.json + README)
3. `079a041` — `chore: remove legacy express tree [Story 8.5] (#5)` (squash-merge of #1+#2 onto master via PR #5)

---

## Follow-up

- Migrate or remove `tests/integration/rate-limit.test.js` (express removed from deps — test fails to import).
- Optionally prune root `package.json` further (drop now-broken `test: jest --runInBand` and `jest` config block; vite/vitest cover frontend tests).
