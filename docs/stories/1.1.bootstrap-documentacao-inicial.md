# Story 1.1: Bootstrap da documentação inicial

## Status
InReview

## Story
**As a** equipa de desenvolvimento,
**I want** uma estrutura documental mínima do projecto (PRD, arquitectura e stories),
**so that** o ciclo AIOX de criação/validação/implementação possa arrancar sem bloqueios.

## Acceptance Criteria
1. Existe a pasta `docs/` no projecto.
2. Existem as pastas `docs/prd`, `docs/architecture` e `docs/stories`.
3. Existe `docs/prd/index.md` com escopo inicial do produto.
4. Existe `docs/architecture/index.md` com visão técnica inicial.
5. Existe uma story inicial em `docs/stories/` para iniciar o ciclo de desenvolvimento.

## Executor Assignment
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["qa-gate", "qa-review-story"]

## CodeRabbit Integration
> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.

## Tasks / Subtasks
- [x] Criar estrutura de documentação base em `docs/`
  - [x] Criar `docs/prd`
  - [x] Criar `docs/architecture`
  - [x] Criar `docs/stories`
- [x] Criar PRD inicial (`docs/prd/index.md`)
- [x] Criar arquitectura inicial (`docs/architecture/index.md`)
- [x] Validar que a story está pronta para fase de validação PO

## Dev Notes
- `core-config.yaml` exige `prdShardedLocation: docs/prd` e `architectureShardedLocation: docs/architecture`.
- O projecto estava sem directório `docs/`, o que bloqueava o passo `create_story`.
- Esta story funciona como desbloqueio operacional do Story Development Cycle.

## Testing
- Verificar existência física dos directórios e ficheiros:
  - `docs/prd/index.md`
  - `docs/architecture/index.md`
  - `docs/stories/1.1.bootstrap-documentacao-inicial.md`

## File List
- `docs/` — directório base de documentação (verificado)
- `docs/prd/` — directório PRD (verificado)
- `docs/prd/index.md` — PRD inicial do produto (verificado)
- `docs/architecture/` — directório de arquitectura (verificado)
- `docs/architecture/index.md` — visão técnica inicial (verificado)
- `docs/stories/` — directório de stories (verificado)
- `docs/stories/1.1.bootstrap-documentacao-inicial.md` — esta story (verificado)

## Dev Agent Record
- **Executor:** @dev (Dex)
- **Mode:** YOLO
- **Date:** 2026-05-31
- **Result:** Todos os artefactos verificados fisicamente. Checkboxes marcados. Sem ficheiros a criar — estrutura já existia.

## Change Log
- 2026-04-24: Story criada manualmente para desbloquear o engine em ambiente greenfield sem documentação inicial.
- 2026-05-31: @po validação concluída — CONDITIONAL GO (7/10). Status alterado de Draft para Ready. Issues registados: ausência de secções Scope, Dependencies, Complexity, Business Value, Risks e DoD (should-fix, não bloqueantes para story de bootstrap). Artefactos verificados como já existentes.
- 2026-05-31: @dev implementação YOLO concluída. Todos os artefactos verificados fisicamente (docs/, docs/prd/index.md, docs/architecture/index.md, docs/stories/). Checkboxes marcados [x]. File List adicionada. Status alterado para InReview.
