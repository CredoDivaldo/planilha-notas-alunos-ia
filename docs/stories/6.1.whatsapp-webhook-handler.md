# Story 6.1: WhatsApp Webhook Handler

**Story ID:** STORY-6.1  
**Epic:** EPIC-6 (AI WhatsApp Chatbot)  
**Status:** Draft  
**Created:** 2026-05-31  
**Updated:** 2026-05-31  

---

## Executor Assignment

executor: "@dev"  
quality_gate: "@qa"  
quality_gate_tools: [manual-review, pytest, npm-lint, npm-typecheck, npm-test]

---

## Story

**As a** student,  
**I want** to send a WhatsApp message to the platform number,  
**so that** the system receives my message and can process it to answer questions about my grades.

---

## Acceptance Criteria

### AC-1: Webhook endpoint is created and secured
**Given:** Evolution API sends a POST to `/api/v1/chatbot/webhook`  
**When:** the request includes the correct `X-Webhook-Token` header  
**Then:** the system returns `200 OK` immediately and queues the message for async processing

### AC-2: Invalid webhook token is rejected
**Given:** a request arrives at `/api/v1/chatbot/webhook`  
**When:** the `X-Webhook-Token` header is missing or incorrect  
**Then:** the system returns `401 Unauthorized` and logs the rejected attempt

### AC-3: Student is identified by phone number
**Given:** the webhook payload contains a sender phone number (`remoteJid`)  
**When:** the system processes the message  
**Then:** it queries the `students` table by normalised phone number and retrieves the matching student record

### AC-4: Unrecognised phone number is handled gracefully
**Given:** the sender phone number does not match any student  
**When:** the system processes the message  
**Then:** it logs the unknown number and does NOT call the AI service or expose any data; optionally sends a polite "number not registered" reply

### AC-5: Non-message events are ignored silently
**Given:** Evolution API sends webhook events that are not text messages (e.g., status updates, read receipts)  
**When:** the system receives such an event  
**Then:** it returns `200 OK` immediately without processing or logging an error

---

## Scope

### IN
- FastAPI endpoint `POST /api/v1/chatbot/webhook`.
- Webhook token validation via `CHATBOT_WEBHOOK_TOKEN` environment variable.
- Phone number normalisation (strip `@s.whatsapp.net`, handle country code).
- Student lookup by normalised phone number.
- Async dispatch to chatbot service (stub acceptable in this story).
- Logging: received message event, student identified / not found.

### OUT
- AI call or grade query (Story 6.2).
- Sending WhatsApp reply (Story 6.3).
- Rate limiting (Story 6.3).
- Multi-turn conversation state.

---

## Dependencies

- Story 5.1: FastAPI backend and SQLite running.
- Story 5.2: `students` table with `phone_number` field.
- Evolution API instance configured with webhook URL pointing to this endpoint.

---

## Risks

| Risk | Mitigation |
|------|------------|
| Evolution API webhook format changes | Parse only documented fields; log raw payload for debugging |
| Phone number format inconsistency | Normalisation function with unit tests covering various formats |
| Webhook endpoint publicly exposed | Token validation mandatory; reject without token immediately |

---

## Tasks / Subtasks

- [ ] Add `CHATBOT_WEBHOOK_TOKEN` to `.env.example` and document in README.
- [ ] Create `backend/app/routers/chatbot.py` with `POST /api/v1/chatbot/webhook`.
- [ ] Implement phone number normalisation utility (`backend/app/utils/phone.py`).
- [ ] Implement token validation middleware/dependency for the webhook route.
- [ ] Implement student lookup by phone number (query `students` table).
- [ ] Stub async dispatch: log message and student ID, return immediately.
- [ ] Handle unknown phone number: log and optionally queue "not registered" reply.
- [ ] Handle non-message events: return 200 silently.
- [ ] Write unit tests for phone normalisation and token validation.
- [ ] Write integration test for webhook endpoint (valid token, invalid token, unknown number).
- [ ] Run `npm run lint`, `npm run typecheck`, `npm test` and `pytest`.

---

## Dev Notes

### Webhook payload structure (Evolution API)
```json
{
  "event": "messages.upsert",
  "instance": "instance-name",
  "data": {
    "key": {
      "remoteJid": "244912345678@s.whatsapp.net",
      "fromMe": false,
      "id": "message-id"
    },
    "message": {
      "conversation": "Qual é a minha nota de Matemática?"
    },
    "messageType": "conversation"
  }
}
```

### Phone normalisation
Strip `@s.whatsapp.net` suffix, strip leading `+` or `00`, normalise to digits only. Store in `students` table in the same normalised format.

### File List
- `docs/stories/6.1.whatsapp-webhook-handler.md`

---

## Testing

- Unit: phone normalisation covers Angola (+244), Portugal (+351) and generic formats.
- Unit: token validation rejects missing and incorrect tokens.
- Integration: valid webhook payload → 200 + student identified in logs.
- Integration: unknown phone → 200 + no data exposed.
- Integration: missing token → 401.
- Regression: `npm run lint`, `npm run typecheck`, `npm test`, `pytest`.

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-05-31 | 1.0 | Story created from correct-course Sprint Change Proposal (Epic 6 — AI requirement) | @aiox-master |
