# Story 7.8 — Calendário (Tela 06)

**Epic:** 7 — Frontend da Plataforma Académica  
**Status:** InReview  
**Priority:** P1  
**Points:** 5  
**Sprint:** 3  
**Depends On:** 7.2, 7.6

---

## Executor Assignment

```
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: [manual-review, npm-lint, npm-typecheck, npm-test]
```

---

## Scope

### IN
- `client/src/pages/professor/CalendarPage.tsx` — professor view with full CRUD (stub exists; replace with real implementation)
- `client/src/pages/student/CalendarPage.tsx` — student read-only view (create new)
- `client/src/components/organisms/EventDetailPanel.tsx` — sidebar panel with role-conditional buttons
- `client/src/components/organisms/EventModal.tsx` — create/edit modal
- `client/src/components/organisms/UpcomingEventsList.tsx` — REUSE from Story 7.6
- `client/src/components/organisms/MonthCalendar.tsx` — ADAPT from Story 7.6 (add `allowEdit` prop, max ~25% change)
- `client/src/components/molecules/EventDot.tsx` — REUSE from Story 7.6
- Router entries for `/calendario` (professor) and `/estudante/calendario` (student)
- Vista Lista (list view) as alternative to monthly grid
- Context selector with "Todos os contextos" aggregate option
- Month navigation switcher (Mês | Lista only — Week view is OUT)
- Colour legend in sidebar
- `.ics` export via manual Blob generation (no `ics` npm package)
- API integration: mock fallback pattern; real endpoints wired via hook when available
- WCAG AA accessibility for all interactive elements

### OUT
- Week view (AC5 mentions "Semana" but no task covers it — deferred; implement Mês + Lista only)
- Push notifications / reminders for events
- Recurring event patterns
- Email/WhatsApp calendar sync
- Backend professor CRUD endpoints (defined in Dev Notes as assumptions; not implemented here)

---

## Story Statement

**As a** professor or student,  
**I want** a monthly calendar showing academic events (exams, tests, deadlines),  
**so that** professors can manage event schedules and students can see upcoming events relevant to their context without being able to edit them.

---

## Acceptance Criteria

- [x] **AC1:** A página `/calendario` renderiza com `AppHeader` (tab "Calendário" activo), título "Calendário Académico", botão "+ Adicionar Evento" (visível apenas para professor) e botão "Exportar ▾" (professor e estudante)
- [x] **AC2:** O selector de contexto permite filtrar por contexto específico ou "Todos os contextos"; ao seleccionar "Todos os contextos", os eventos de todos os contextos do professor são agregados no mesmo calendário
- [x] **AC3:** A grelha de calendário mensal mostra os dias do mês com EventDots coloridos por tipo: 🔵 Prova (`#3B82F6`), 🟣 Exame (`#8B5CF6`), 🟠 Recurso (`#F97316`), 🟢 Entrega (`#22C55E`), 🔴 Prova Extra (`#EF4444`)
- [x] **AC4:** Navegação de mês: setas ◀ ▶ mudam o mês sem reload; o mês e ano actuais são exibidos no cabeçalho da grelha
- [x] **AC5:** Vista alternativa: o switcher "Mês | Lista" muda entre vistas; a vista Lista mostra eventos ordenados por data (útil em mobile). **Nota: vista Semana está fora de âmbito desta story — ver Scope OUT.**
- [x] **AC6:** Clicar num evento (dot ou dia com evento) abre o painel lateral direito com: data, badge colorido de tipo, descrição, horário (início–fim), local, contexto; professor vê botões "✏️ Editar evento" e "🗑 Eliminar"; estudante vê apenas o detalhe sem botões de edição
- [x] **AC7:** A lista "Próximos Eventos (7 dias)" abaixo do calendário mostra os eventos dos próximos 7 dias com: data, tipo badge, descrição, hora, sala
- [x] **AC8:** **Vista Professor — Criar Evento:** modal "📅 Criar Evento Académico" com campos: Tipo de evento (RadioGroup: Prova, Exame, Recurso, Entrega, Prova Extra com cor indicada), Descrição (obrigatório), Contexto (Select, obrigatório), Data (date picker, obrigatório), Hora início, Hora fim, Local, Notas internas (facultativo), toggle "Visível para estudantes" (Sim/Não); ao submeter, chama o endpoint de criação de evento
- [x] **AC9:** **Vista Professor — Editar Evento:** mesmo modal com dados pré-preenchidos
- [x] **AC10:** **Vista Professor — Eliminar Evento:** Dialog de confirmação "Eliminar o evento [Descrição]?"; confirmar remove o evento do calendário
- [x] **AC11:** Apenas eventos com `visivel_estudantes: true` aparecem na vista do estudante e no Portal do Estudante (Story 7.6); o toggle no modal controla este flag
- [x] **AC12:** O botão "Exportar ▾" gera um ficheiro `.ics` (iCal) com os eventos visíveis, compatível com Google Calendar e Outlook
- [x] **AC13:** O `MonthCalendar` é o mesmo componente criado na Story 7.6 — reutilizado aqui com prop `allowEdit={role === 'professor'}`
- [x] **AC14:** A legenda de cores está sempre visível na sidebar direita
- [x] **AC15:** A página é acessível WCAG AA: EventDots são `<button>` com `aria-label` descritivo; modal tem focus trap; RadioGroup de tipo de evento tem `role="radiogroup"`

---

## Tasks

- [x] **T1:** Substituir stub em `client/src/pages/professor/CalendarPage.tsx` (stub já existe — implementar vista completa do professor) e criar `client/src/pages/student/CalendarPage.tsx` (vista estudante, read-only)
- [x] **T2:** Adaptar `client/src/components/organisms/MonthCalendar.tsx` da Story 7.6: adicionar prop opcional `allowEdit?: boolean` e `onEventClick?: (event: CalendarEvent) => void`; manter retrocompatibilidade total com Story 7.6 (mudanças < 30%)
- [x] **T3:** Implementar vista Lista (`ListViewPanel.tsx` inline ou secção na CalendarPage) como alternativa ao calendário mensal (eventos ordenados por data, útil em mobile) — AC5
- [x] **T4:** Implementar selector de contexto com opção "Todos os contextos" para professor — AC2
- [x] **T5:** Criar `client/src/components/organisms/EventDetailPanel.tsx` (sidebar direita) com detalhe + botões condicionais por role — AC6, AC14
- [x] **T7:** Implementar RadioGroup de tipo de evento com indicadores de cor por opção (usar `client/src/components/ui/radio-group.tsx`) — AC8, AC15
- [x] **T6:** Criar `client/src/components/organisms/EventModal.tsx` com formulário criar/editar (depende de T7 — RadioGroup deve estar pronto) — AC8, AC9
- [x] **T12:** Criar componente `ColorLegend` (inline na sidebar) com legenda de cores dos tipos de evento — AC14
- [x] **T13:** Implementar switcher de vista "Mês | Lista" e navegação de mês (← →) — AC4, AC5
- [x] **T8:** Integrar endpoint de criação de evento com mock fallback (ver Dev Notes — Endpoints Professor) — AC8
- [x] **T9:** Integrar endpoint de edição de evento com mock fallback — AC9
- [x] **T10:** Integrar endpoint de eliminação de evento com Dialog de confirmação shadcn/ui — AC10
- [x] **T11:** Implementar geração de ficheiro `.ics` para exportação via `Blob` + `URL.createObjectURL` (sem dependência externa) — AC12
- [x] **T_ROUTER:** Adicionar rotas em `client/src/router/index.tsx`: `/calendario` (professor, via `PrivateRoute`) e `/estudante/calendario` (student, via `StudentRoute`) — AC1

---

## Dev Notes

### Wireframe de Referência
**Ficheiro:** `outputs/wireframes/planilha-notas/screens/06-calendario.md`

O wireframe define:
- Layout 1280px: header + toolbar (título + botões) + selector de contexto
- Layout 2 colunas: 70% calendário + 30% sidebar (legenda + evento seleccionado)
- Grelha mensal com EventDots coloridos; lista de próximos 7 dias abaixo
- Modal criar/editar: RadioGroup de tipo + campos de data/hora/local/contexto + toggle estudante
- Variante estudante: mesmo layout, sem botões de edição, com botão "Descarregar .ics"

### Componentes do Inventário a Usar

| ID Inventário | Componente | Ficheiro | Acção |
|--------------|-----------|---------|-------|
| O06 | MonthCalendar | `client/src/components/organisms/MonthCalendar.tsx` (Story 7.6) | ADAPT — adicionar `allowEdit` e `onEventClick` props |
| O07 | UpcomingEventsList | `client/src/components/organisms/UpcomingEventsList.tsx` (Story 7.6) | REUSE — sem modificações |
| M07 | EventDot | `client/src/components/molecules/EventDot.tsx` (Story 7.6) | REUSE — sem modificações |
| M08 | CalendarDay | inline em MonthCalendar | — |
| M09 | RadioGroup | `client/src/components/ui/radio-group.tsx` (shadcn/ui, confirmado) | REUSE |
| T05 | CalendarLayout | Não usar `layouts/` — integrar directamente na CalendarPage | — |
| O01 | AppHeader | `client/src/components/organisms/AppHeader.tsx` | REUSE |

> **Nota IDS:** MonthCalendar interface actual: `{ events: CalendarEvent[] }` — não tem `allowEdit`. T2 deve adicionar estas props com retrocompatibilidade. Verificar se Story 7.6 usa MonthCalendar sem `allowEdit` antes de modificar (não quebrar consumers existentes).

### MonthCalendar — Adaptação Necessária (T2)

Interface actual (Story 7.6):
```typescript
interface MonthCalendarProps {
  events: CalendarEvent[]
}
```

Interface target (Story 7.8):
```typescript
interface MonthCalendarProps {
  events: CalendarEvent[]
  allowEdit?: boolean          // default: false — habilita click callbacks para edição
  onEventClick?: (event: CalendarEvent) => void  // callback para EventDetailPanel
}
```

Regra de retrocompatibilidade: `allowEdit` e `onEventClick` são opcionais com defaults. Story 7.6 continua a funcionar sem modificações.

### Cores de Tipos de Evento

| Tipo | Cor | Valor Hex |
|------|-----|-----------|
| Prova | Azul | `#3B82F6` |
| Exame | Roxo | `#8B5CF6` |
| Recurso | Laranja | `#F97316` |
| Entrega | Verde | `#22C55E` |
| Prova Extra | Vermelho | `#EF4444` |

### Endpoints API Relevantes

**Endpoint estudante (confirmado — Story 5.6, Done):**

| Endpoint | Método | Uso |
|----------|--------|-----|
| `GET /api/v1/portal/me/calendar` | GET | Eventos publicados para estudante autenticado (apenas `is_published=True`, scoped ao contexto do estudante) |

**Endpoints professor CRUD (não documentados em Epic 5 — Story 5.6 excluiu professor CRUD do âmbito):**

> **ASSUNÇÃO DO DEV** (implementar com mock fallback; wired para real endpoint quando disponível):

| Endpoint (assumido) | Método | Payload / Uso |
|--------------------|--------|--------------|
| `GET /api/v1/calendar/events` | GET | Lista eventos do professor; query params: `context_id?`, `start_date?`, `end_date?` |
| `POST /api/v1/calendar/events` | POST | Criar evento: `{ type, description, context_id, date, start_time?, end_time?, location?, notes?, visivel_estudantes }` |
| `PUT /api/v1/calendar/events/:id` | PUT | Editar evento (mesmo payload que POST) |
| `DELETE /api/v1/calendar/events/:id` | DELETE | Eliminar evento |

**Padrão de mock fallback** (consistente com Story 7.3, 7.5):
```typescript
// Usar dados mock em dev; substituir por fetch real quando endpoint disponível
const [events, setEvents] = useState<CalendarEvent[]>(MOCK_EVENTS)

// Para CRUD: actualizar estado local (optimistic update)
// Quando API estiver disponível: await fetch('/api/v1/calendar/events', { method: 'POST', ... })
```

**Campo `visivel_estudantes`:** mapeado para `is_published` no modelo `published_calendar_snapshots` (Story 5.6). Quando `true`, evento aparece em `GET /api/v1/portal/me/calendar`.

### CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.
> To enable, set `coderabbit_integration.enabled: true` in core-config.yaml

### Geração de `.ics`

O formato iCal básico:
```
BEGIN:VCALENDAR
VERSION:2.0
BEGIN:VEVENT
DTSTART:20260617T090000
DTEND:20260617T120000
SUMMARY:Exame Final Inglês
LOCATION:Sala 3
END:VEVENT
END:VCALENDAR
```
Usar `Blob` + `URL.createObjectURL` para download sem servidor.

### Acessibilidade

- EventDots: `<button type="button" aria-label="17 Junho - Exame Final Inglês Técnico">` — nunca apenas `<div>`
- Modal: focus trap, `aria-labelledby="event-modal-title"`, Escape fecha
- RadioGroup de tipos: `<fieldset><legend>Tipo de evento</legend>...` com cor também em texto (não só cor)
- Calendário: dias sem eventos têm `aria-label="5 de Junho, sem eventos"`

---

## Testing

### Cenários de Teste Multi-Role
- [ ] Professor acede a `/calendario`: vê calendário com botão "+ Adicionar Evento" e "Exportar ▾"
- [ ] Estudante acede a `/estudante/calendario`: vê calendário sem botões de edição; apenas eventos com `visivel_estudantes: true`
- [ ] Utilizador não autenticado: redireccionado para `/login`

### CRUD de Eventos (Professor)
- [ ] Criar evento: preencher modal, submeter — evento aparece no calendário com EventDot colorido correcto
- [ ] Editar evento: clicar em EventDot → EventDetailPanel → "Editar" → modal pré-preenchido → guardar
- [ ] Eliminar evento: clicar "Eliminar" → Dialog de confirmação → confirmar → evento removido
- [ ] Toggle `visivel_estudantes`: criar evento com toggle OFF → não visível na vista estudante; toggle ON → visível

### Vista Lista
- [ ] Switcher "Mês | Lista": clicar "Lista" → eventos ordenados por data sem grelha de calendário
- [ ] Vista Lista em mobile (< 768px): layout stacked, sem grelha mensal

### Exportação .ics
- [ ] Clicar "Exportar ▾" → download de ficheiro `calendario.ics` com eventos visíveis
- [ ] Ficheiro `.ics` válido: abrir em Google Calendar / Outlook (validar formato BEGIN:VCALENDAR)

### Navegação de Mês
- [ ] Setas ◀ ▶: navegar para mês anterior/seguinte sem reload; cabeçalho actualiza
- [ ] Cruzar anos: Dezembro → Janeiro (ano +1), Janeiro → Dezembro (ano -1)

### Acessibilidade WCAG AA
- [ ] EventDots têm `aria-label` descritivo (data + título)
- [ ] Modal tem focus trap — Tab não sai do modal enquanto aberto; Escape fecha
- [ ] RadioGroup de tipo de evento tem `role="radiogroup"` e `<legend>`

---

## File List

| Ficheiro | Acção |
|---------|-------|
| `client/src/pages/professor/CalendarPage.tsx` | MODIFY (stub existente — implementar conteúdo completo) |
| `client/src/pages/student/CalendarPage.tsx` | CREATE |
| `client/src/components/organisms/EventDetailPanel.tsx` | CREATE |
| `client/src/components/organisms/EventModal.tsx` | CREATE |
| `client/src/components/organisms/MonthCalendar.tsx` | ADAPT (adicionar props `allowEdit`, `onEventClick`) |
| `client/src/components/organisms/UpcomingEventsList.tsx` | REUSE (sem modificações) |
| `client/src/components/molecules/EventDot.tsx` | REUSE (sem modificações) |
| `client/src/router/index.tsx` | MODIFY (adicionar rotas `/calendario` e `/estudante/calendario`) |

---

## Change Log

| Data | Autor | Descrição |
|------|-------|-----------|
| 2026-06-05 | @sm (River) | Story criada a partir do handoff epic 7 e wireframe 06-calendario.md |
| 2026-06-07 | @po (Pax) | Validação PO — GO 8/10. Fixes aplicados: Status Draft→Ready; Points 3→5; Depends On 7.6 adicionado; Scope IN/OUT criado; vista Semana movida para OUT em AC5; Executor Assignment secção formal adicionada; quality_gate_tools especificados; File List corrigido (packages/frontend→client/src) e expandido de 3→8 entradas com REUSE/ADAPT/MODIFY; professor CRUD endpoints documentados com assunções concretas; MonthCalendar allowEdit gap documentado com interface target; T6/T7 reordenados (T7 antes T6); T_ROUTER adicionado; Testing section criada; CodeRabbit skip notice adicionado; inventory component paths corrigidos. |
| 2026-06-07 | @dev (Dex) | Implementação completa — YOLO mode. Todos 14 tasks [x]. MonthCalendar adaptado (allowEdit, onEventClick, selectedEventId — retrocompat preservada). EventModal criado com RadioGroup T7. EventDetailPanel criado com ColorLegend inline. ProfessorCalendarPage implementada (CRUD completo, context selector, Month+List switcher, .ics export, mock fallback). StudentCalendarPage criada (read-only, student endpoint). Router atualizado com /calendario e /estudante/calendario. typecheck PASS, tests 219/219 PASS. Status: InReview. |
