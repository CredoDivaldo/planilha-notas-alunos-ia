{
  "name": "planilha-notas-alunos-backend",
  "version": "0.1.0",
  "private": true,
  "description": "FastAPI backend for the local academic platform",
  "scripts": {
    "dev": "cd .. && .venv/bin/python -m uvicorn backend.app.main:app --reload --port 8000"
  }
}
